apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-clusterissuer-bitbark-backend-{{ .Values.namespace}}
spec:
  acme:
    email: {{ .Values.clusterissuer.acm.email}}
    server: https://acme-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: letsencrypt-secret-bitbark-backend-{{ .Values.namespace}}
    solvers:
      - http01:
          ingress:
            class: nginx