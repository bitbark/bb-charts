apiVersion: v1
kind: Service
metadata:
  name: bitbark-frontend
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: bitbark-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80